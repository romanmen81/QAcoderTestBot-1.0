
📌 Подробная инструкция по настройке и запуску Telegram-бота с викториной

Данный бот реализует интерактивную викторину с вопросами и автоматизированным счётом очков. Пользователи могут пройти тестирование, отвечая на ряд вопросов, и в конце получают итоговый результат.

🔧 Предварительные требования
Перед началом убедитесь, что у вас установлены:

Python (версия ≥ 3.8)
Менеджер виртуальных сред (например, virtualenv)
Редактор кода (например, VS Code или PyCharm)

📌 Установка и настройка проекта

1. Клонирование репозитория
Клонируйте репозиторий на ваш компьютер:

git clone https://github.com/<username>/<project-name>.git
cd <project-name>

2. Создание виртуального окружения
Рекомендуется использовать виртуальное окружение для изолированной работы с зависимостями проекта:

python -m venv env
source env/bin/activate  # Linux/macOS
env\Scripts\activate.bat  # Windows

3. Установка зависимостей
Установите необходимые библиотеки с помощью команды:

pip install -r requirements.txt

4. Создание файла .env
Для хранения секретных данных (токена бота) создайте файл .env в корне проекта с таким содержимым:

TELEGRAM_BOT_TOKEN=ТокенБотаТелеграм

5. Настройка базы данных
Бот использует SQLite в качестве базы данных. Файл базы данных (questions.db) будет создан автоматически при первом запуске. Если у вас есть готовые вопросы, можно заранее наполнить базу данных, используя скрипт add_sqlite.py.

6. Запуск бота
Запустите основной скрипт бота:

python main.py

📌 Запуск и эксплуатация бота

📌 Первый запуск

После запуска бота в Telegram введите команду /start, чтобы начать тестирование. Вы увидите меню с кнопками:
Старт: запускает викторину.
Об викторине: описывает правила и цели викторины.

📌 Прохождение викторины

После нажатия кнопки «Старт» вам начинают поступать вопросы с четырьмя вариантами ответов. Выберите один из предложенных вариантов, нажав на соответствующую кнопку.

📌 Завершение викторины

После прохождения 10 вопросов (или исчерпания базы данных) вы получите итоговый результат, отражающий количество правильных ответов.

📌 Дополнительно полезные скрипты

add_sqlite.py: позволяет вручную добавлять вопросы в базу данных.
del_sqlite.py: удаляет вопросы из базы данных по их ID.
report_sqlite.py: выводит список всех вопросов с ответами в консоль и сохраняет их в файл report.txt.
optim_sqlite.py: утилита для оптимизации базы данных (исправляет перебои в нумерации ID, но рекомендуется осторожно использовать и предварительно создать резервную копию).

📌 Распространённые ошибки и их устранение

Ошибка: ModuleNotFoundError: No module named 'telegram'
Решение: Проверьте, установлена ли библиотека python-telegram-bot. Установите её командой pip install python-telegram-bot.

Ошибка: sqlite3.OperationalError: no such table: questions
Решение: Убедитесь, что база данных создана. Она должна появиться автоматически при первом запуске.

Ошибка: Invalid Token
Решение: Проверьте правильность токена бота в файле .env.

📌 Рекомендуемые улучшения

Автозаполнение базы данных: добавление готовых вопросов при первом запуске.
Режимы игры: введение тренировочных и соревновательных режимов.
Интеграция голосования: возможность собирать отзывы и оценки пользователей.
Данная инструкция позволит любому человеку легко установить и настроить ваш Telegram-бот с викториной, а также эффективно его использовать.
